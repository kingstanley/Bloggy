import { Subscription.Repository } from './subscription.repository';

describe('Subscription.Repository', () => {
  it('should be defined', () => {
    expect(new Subscription.Repository()).toBeDefined();
  });
});
